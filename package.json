{
  "name": "node-oauth2-jwt-workshop",
  "version": "1.0.0",
  "description": "OAuth2 Server mit JWT Tokens",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.13.0",
    "express": "^4.12.4",
    "jsonwebtoken": "^5.0.2",
    "oauth2-server": "^2.4.0"
  }
}
